---
description: Implement a specific task with clean architecture, clean code, and unit tests
---

There is a subagent available called @implementer that specializes in
implementing atomic tasks with clean architecture, clean code, and full comments.

Invoke the implementer subagent now. It will detect the tech stack, load the
appropriate language skill, read the task from `docs/pipeline/tasks/task-<N>.md`, and
implement it with corresponding unit tests.

$ARGUMENTS
